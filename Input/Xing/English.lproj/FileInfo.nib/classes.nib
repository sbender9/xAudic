{
    IBClasses = (
        {
            ACTIONS = {removeId3 = id; }; 
            CLASS = FileInfo; 
            LANGUAGE = ObjC; 
            OUTLETS = {tableView = id; }; 
            SUPERCLASS = NibObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {cancel = id; ok = id; show = id; }; 
            CLASS = NibObject; 
            LANGUAGE = ObjC; 
            OUTLETS = {window = id; }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}