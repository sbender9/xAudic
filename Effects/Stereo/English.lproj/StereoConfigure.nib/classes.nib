{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {cancel = id; ok = id; show = id; }; 
            CLASS = NibObject; 
            LANGUAGE = ObjC; 
            OUTLETS = {window = id; }; 
            SUPERCLASS = NSObject; 
        }, 
        {
            ACTIONS = {intensityChanged = id; }; 
            CLASS = StereoConfigure; 
            LANGUAGE = ObjC; 
            OUTLETS = {intensitySlider = id; intensityText = id; }; 
            SUPERCLASS = NibObject; 
        }
    ); 
    IBVersion = 1; 
}